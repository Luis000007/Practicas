package Java.Intermedio.Programacion_orientada_a_objetos;

public class Explicacion {

    /*
    Java utiliza la Programación Orientada a Objetos (POO), un estilo de programación que tiene como objetivo hacer que pensar en programación sea más similar a pensar en el mundo real.

En POO, cada objeto es una unidad independiente con una identidad única, al igual que los objetos en el mundo real.

Una manzana es un objeto; también lo es una taza. Cada uno tiene su identidad única. Es posible tener dos tazas que se vean idénticas, pero aún así son objetos separados y únicos.

Los objetos también tienen características, que se utilizan para describirlos.

Por ejemplo, un coche puede ser rojo o azul, una taza puede estar llena o vacía, y así sucesivamente. Estas características también se llaman atributos. Un atributo describe el estado actual de un objeto.

En el mundo real, cada objeto se comporta de su propia manera. El coche se mueve, el teléfono suena, y así sucesivamente.

Lo mismo se aplica a los objetos: el comportamiento es específico para el tipo de objeto.

En resumen, en la programación orientada a objetos, cada objeto tiene tres dimensiones: identidad, atributos y comportamiento.

Los atributos describen el estado actual del objeto, y lo que el objeto es capaz de hacer se demuestra a través del comportamiento del objeto.

Clases
Una clase describe lo que será el objeto, pero es independiente del objeto en sí.

En otras palabras, las clases pueden describirse como planos, descripciones o definiciones de un objeto. Puedes usar la misma clase como un plano para crear múltiples objetos. El primer paso es definir la clase, que luego se convierte en un plano para la creación de objetos.

Cada clase tiene un nombre, y cada una se utiliza para definir atributos y comportamiento.

En otras palabras, un objeto es una instancia de una clase.
     */
}
